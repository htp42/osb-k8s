---
# Clinical MDR API Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: openstudybuilder
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: openstudybuilder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
        app.kubernetes.io/name: api
        app.kubernetes.io/component: backend
    spec:
      initContainers:
        - name: wait-for-database
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z database 7687; do echo waiting for database; sleep 5; done;']
      containers:
        - name: api
          image: saurcode/osb-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5003
              protocol: TCP
          env:
            - name: NEO4J_DSN
              value: "bolt://neo4j:changeme1234@database:7687/mdrdb"
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: osb-secrets
                  key: NEO4J_PASSWORD
            - name: UVICORN_PORT
              value: "5003"
            - name: UVICORN_ROOT_PATH
              value: "/api"
            - name: UVICORN_APP
              value: "clinical_mdr_api.main:app"
            - name: ALLOW_ORIGIN_REGEX
              valueFrom:
                configMapKeyRef:
                  name: osb-config
                  key: ALLOW_ORIGIN_REGEX
            - name: OAUTH_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: osb-config
                  key: OAUTH_ENABLED
            - name: OAUTH_RBAC_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: osb-config
                  key: OAUTH_RBAC_ENABLED
            - name: POCKETBASE_PUBLIC_URL
              valueFrom:
                configMapKeyRef:
                  name: osb-config
                  key: POCKETBASE_PUBLIC_URL
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /system/healthcheck
              port: 5003
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /system/healthcheck
              port: 5003
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
---
# API Service
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: openstudybuilder
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: openstudybuilder
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5003
      targetPort: 5003
      protocol: TCP
  selector:
    app: api
